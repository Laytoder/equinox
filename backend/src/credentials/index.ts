import credentials from "../../../common/credentials.json";

const apiKey = credentials.apiKey;
const apiSecret = credentials.apiSecret;
const paper = credentials.paper;

export { apiKey, apiSecret, paper };
